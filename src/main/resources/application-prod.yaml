spring:
  config:
    import: optional:classpath:application-secret.yaml
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    org.hibernate.SQL: info # 프로덕션에서는 성능을 위해 불필요한 쿼리 로그 제거